const name = "Susan";  // Update to "Susan" as required by the test
const height = 30;  // Make sure height is less than 40
const message = `${name} is ${height} inches tall`;

module.exports = { name, height, message };
